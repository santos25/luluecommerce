const { makeStyles } = require("@material-ui/core");

export default makeStyles((theme) => ({
  root: {
    maxWidth: 545,
    margin: theme.spacing(0.5),
  },
  media: {
    height: 340,
  },
  icon: {
    fontSize: 26,
  },
}));
