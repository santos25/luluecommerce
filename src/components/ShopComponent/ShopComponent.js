import React from 'react';
import CardImages from '../CardImages/CardImages';

const ShopComponent = ({  item }) => {
    return (
            <CardImages  item={item}/>
        );
}

export default ShopComponent;